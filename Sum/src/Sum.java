/**
 * 
 */

/**
 * @author gferraz
 *
 */
public class Sum {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		int n = 10;
		int n2 = 6;
		int result = n + n2;
		
		System.out.println("The sum of " + n + " and " + n2 + " is " + result);

		int totalVotes = 100000;
		int myCandidateVotes = 30000;
		int myCandidatePercetage = (myCandidateVotes * 100) / totalVotes;
		
		System.out.println("My candidate's percentage of votes is " + myCandidatePercetage + "%");
		
		
	}

}
