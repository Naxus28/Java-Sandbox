import javax.swing.JOptionPane;

/**
 * 
 */

/**
 * @author gabrielferraz
 *
 */
public class PayCalculator {
	
	/**
	 * class globals
	 */
	final int incentiveLeveOne = 5;
	final int incentiveLevelTwo = 10;
	final int incentiveLevelThree = 15;
	static String knives;
	static String inputDirections = "Please input the number of knoves you sold this month (value needs to be less than 1,000,000)";
	
	/**
	 * @param args
	 */
	public static void main(String[] args) {
		double totalPay;
		
		/**
		 * get knives #
		 */
		do {
			knives = JOptionPane.showInputDialog(inputDirections);
			JOptionPane.showMessageDialog(null, knives, "Knives sold this month", JOptionPane.PLAIN_MESSAGE);
		}
		while(knives.length() == 0 || Double.parseDouble(knives) > 1000000 || Double.parseDouble(knives) > 1000000);
		
		if (soldKnives <= 100) {
			totalPay = soldKnives * incentiveLeveOne;
		} else if (soldKnives > 100 && soldKnives <= 200) {
			totalPay = 
		}
	}
	
	static private double getTotalPay(soldKnives) {
		double totalPay;
		
		if (soldKnives <= 100) {
			totalPay = soldKnives * incentiveLeveOne;
		} else if (soldKnives > 100 && soldKnives <= 200) {
			totalPay = 
		}
		
		return totalPay;
	}

}
